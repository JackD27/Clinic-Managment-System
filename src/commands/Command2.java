package commands;

/**
 * Interface for Command2 class. Contains a method to execute the command. This is Milestone 4 now.
 */
public interface Command2 {
  /**
   * Executes the command. Throws an IllegalArgumentException if the command cannot be executed.
   */
  void execute() throws IllegalArgumentException;
}
